package KPI

import java.util.ArrayList

object test {

  

  def main(args: Array[String]) {

    var data = List()
    
val arr = Array(1,3,6,12,24)

    var contador = 0
var rdd = Array[String]

    for (name <- arr) {

      rdd(contador) = "fsdggfg" + name.toString()
      println(arr(contador))
    }
    
    //println(arr)

  }
}