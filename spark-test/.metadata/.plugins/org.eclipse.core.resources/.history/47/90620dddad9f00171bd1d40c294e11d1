package KPI

object test {

  

  def main(args: Array[String]) {
    
    val rangoVisitas = List(1, 3, 6, 12, 24)
    var data = Array()
    
    
    
    var contador = 0
    for (name <- rangoVisitas) {
      data[contador] = name +1
      //contador++
    }

  }
}