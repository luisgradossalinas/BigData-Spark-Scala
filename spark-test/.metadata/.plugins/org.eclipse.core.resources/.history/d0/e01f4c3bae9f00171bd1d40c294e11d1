package KPI

object test {

  

  def main(args: Array[String]) {
    
    val rangoVisitas = List(1, 3, 6, 12, 24)
    var data = List()
    
    
    
    var contador = 0
    for (name <- rangoVisitas) {
      data.addString(name.toString())
      //contador++
    }

  }
}