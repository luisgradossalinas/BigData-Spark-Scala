package KPI

import java.util.ArrayList

object test {

  

  def main(args: Array[String]) {


    
var arr = Array(1,3,6,12,24)

var v = new Array[String](arr.length)

    var contador = 0
//var rdd = Array[]

    for (name <- arr) {

      //rdd(contador) = "fsdggfg" + name.toString()
      //var x<name> = 1
      
      v(contador) = "martin" + arr
      println(v(contador))
      contador++
      
      //println(arr(contador))
    }
    
    //println(v)

  }
}